[package]
name = "first-kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
linked_list_allocator = "0.10"
bootloader = "0.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

